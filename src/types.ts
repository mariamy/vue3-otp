export interface DefaultOptions {
    inputsCount: 4,
    focusOnLoad: boolean;
    disableArrows: boolean;
    focusNextOnInput: boolean;
}
export interface CustomProps {
    inputsCount?: number;
    focusOnLoad?: boolean;
    disableArrows?: boolean;
    focusNextOnInput?: boolean;
}
